---
title: SDK Overview
description: Official Logstack SDKs for JavaScript/TypeScript, Go, and Python.
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

# SDK Overview

Logstack ships **three official SDKs**. All talk to the same ingest API
(`POST /v1/logs`), support structured metadata, batching, and optional automatic
capture of existing logger / console output.

## Choose your language

<Cards>
  <Card
    title="JavaScript / TypeScript"
    description="logstack-js — Node, browsers, Next.js, Express, NestJS"
    href="/docs/sdk/javascript"
  />
  <Card
    title="Go"
    description="logstack-go-sdk — services, workers, stdlib log capture"
    href="/docs/sdk/go"
  />
  <Card
    title="Python"
    description="logstack-py — apps, Django, FastAPI, stdlib logging"
    href="/docs/sdk/python"
  />
</Cards>

## Install

<Tabs items={["JavaScript", "Go", "Python"]}>
  <Tab value="JavaScript">

```bash
npm install logstack-js
# or: pnpm add logstack-js / yarn add logstack-js
```

  </Tab>
  <Tab value="Go">

```bash
go get github.com/mosesedem/logstack/packages/logstack-go-sdk@v1.0.3
```

  </Tab>
  <Tab value="Python">

```bash
pip install logstack-py
```

  </Tab>
</Tabs>

## First log

<Tabs items={["JavaScript", "Go", "Python"]}>
  <Tab value="JavaScript">

```typescript
import { createLogStack } from "logstack-js";

const logstack = createLogStack({
  apiKey: process.env.LOGSTACK_API_KEY!,
});

logstack.info("User signed up", { userId: "user_123" });
await logstack.close();
```

  </Tab>
  <Tab value="Go">

```go
client := logstack.NewClient(logstack.Config{
	APIKey: os.Getenv("LOGSTACK_API_KEY"),
})
defer client.Close()

_ = client.Info(context.Background(), "User signed up", map[string]interface{}{
	"userId": "user_123",
})
```

  </Tab>
  <Tab value="Python">

```python
from logstack import LogStackClient

with LogStackClient(api_key=os.environ["LOGSTACK_API_KEY"]) as client:
    client.info("User signed up", metadata={"userId": "user_123"})
```

  </Tab>
</Tabs>

## Shared concepts

| Concept | Behavior |
| --- | --- |
| **API key** | Project key from the dashboard (`ls_live_…`) |
| **Endpoint** | Host only (e.g. `https://api.logstack.tech`); SDKs append `/v1/logs` |
| **Levels** | `debug`, `info`, `warn`, `error`, `critical`, `fatal` |
| **Metadata** | JSON object — searchable in the dashboard |
| **Batching** | Buffer + interval flush (defaults: 100 entries / ~5s) |
| **Environment** | `production` / `staging` / `development` / `test` label on each batch |
| **Auto-capture** | JS: `console.*` · Go: stdlib `log` · Python: stdlib `logging` |

<Callout type="info">
  Logs are shipped in <strong>every</strong> environment when an API key is set.
  Console / local logger output is separate from network shipping.
</Callout>

## Package map

| Language | Package | Source |
| --- | --- | --- |
| JavaScript / TypeScript | [`logstack-js`](https://www.npmjs.com/package/logstack-js) | `packages/logstack-js` |
| Go | [`…/logstack-go-sdk`](https://pkg.go.dev/github.com/mosesedem/logstack/packages/logstack-go-sdk) | `packages/logstack-go-sdk` |
| Python | [`logstack-py`](https://pypi.org/project/logstack-py/) (import `logstack`) | `packages/logstack-python` |

## Guides

- [JavaScript SDK (detailed)](/docs/sdk/javascript) — config, levels, close/flush
- [Go SDK](/docs/sdk/go) — Config, context methods, stdlib capture
- [Python SDK](/docs/sdk/python) — client, Django, FastAPI
- [Configuration (JS deep-dive)](/docs/sdk/configuration)
- [Logging best practices](/docs/sdk/logging)
- [Framework integrations](/docs/sdk/frameworks)
- [HTTP Logs API](/docs/api/logs)
